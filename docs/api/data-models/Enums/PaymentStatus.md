# PaymentStatus

**Source:** `lyda-shared/src/Enums/PaymentStatus.ts`

## PaymentStatus

**Type:** enum

```typescript
export enum PaymentStatus {
    requested = "requested",
    paid = "paid",
    failed = "failed",
    processing = "processing",
}
```

